import NavLinks from "./NavLinks";
import logo from "../images/logo.svg";

const Header = () => {
  return (
    <header className="header">
    <h5 className="logo">Rohit</h5>
      <NavLinks />
    </header>
  );
};

export default Header;
